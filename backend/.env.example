# ============================================================
# 还我一个土木梦 - 后端环境变量配置
# ============================================================
# 复制此文件为 .env 并根据环境修改配置
# cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# 服务器配置
# ------------------------------------------------------------
PORT=3001
HOST=localhost
NODE_ENV=development

# ------------------------------------------------------------
# 安全配置（生产环境必须修改）
# ------------------------------------------------------------
# JWT 密钥：生产环境请使用强随机字符串（至少 32 位）
# 生成方式: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-change-in-production

# ------------------------------------------------------------
# 数据库配置
# ------------------------------------------------------------
# SQLite 默认存储在 data/game.db
# DATABASE_URL=sqlite://./data/game.db

# ------------------------------------------------------------
# 限流配置
# ------------------------------------------------------------
# RATE_LIMIT_WINDOW: 限流时间窗口（毫秒）
# RATE_LIMIT_MAX: 时间窗口内最大请求数
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# ------------------------------------------------------------
# LLM 配置（可选 - 用于动态事件生成）
# ------------------------------------------------------------
# 不配置 LLM 时，游戏仍可正常运行，但不会生成动态事件
#
# 支持的提供商: openai | deepseek | anthropic | doubao
LLM_PROVIDER=doubao

# API Key（根据提供商获取）
# - 豆包（推荐）: https://www.volcengine.com/
# - DeepSeek: https://platform.deepseek.com/api_keys
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/
LLM_API_KEY=your-api-key-here

# API 端点（可选，默认使用提供商默认值）
# 豆包需要配置此参数
LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 模型名称（可选，使用提供商默认模型）
LLM_MODEL=doubao-seed-1-6-lite-251015

# 推理强度（仅豆包支持，可选: low|medium|high）
LLM_REASONING_EFFORT=medium
