# 还我一个土木梦 - 重大玩法重构设计

**日期**: 2026-01-25
**类型**: 核心玩法重构
**实施方式**: Git Worktree 全新分支开发

---

## 设计概述

### 核心改动

1. **游戏结束条件修改** - 移除现金不足导致游戏结束
2. **游戏分阶段系统** - 前期个人奋斗 vs 后期团队管理
3. **行动点系统** - 基于健康的体力制
4. **团队系统** - 成员招募、管理、领导力
5. **事件系统重构** - 从主导机制改为弹窗通知
6. **UI 重构** - 底部导航栏设计

---

## 一、核心架构设计

### 1.1 行动点-季度系统

**双层结构**：
- 外层：**季度**（时间单位），游戏时间跨越约 40-60 季度（10-15年）
- 内层：**行动点**（直接消耗，无"回合"概念）

**每季度流程**：
```
第 1 季度开始
  ├─ 行动点初始化：5/5（基于健康值 Math.ceil(健康/20)）
  ├─ 执行行动："做项目" → 行动点 4/5
  ├─ 执行行动："培训学习" → 行动点 3/5
  ├─ 执行行动："休息" → 行动点 2/5，健康恢复
  ├─ ...（继续消耗行动点）
  ├─ 行动点归零 OR 玩家主动点击"完成季度"
  └─ 进入季度结算

第 2 季度开始
  ├─ 行动点重新计算（基于当前健康）
  └─ ...
```

**行动点计算**：
```
每季度行动点 = Math.ceil(当前健康 / 20)
健康 100 → 5点
健康 60  → 3点
健康 20  → 1点
```

**健康恢复**（混合机制）：
- 每季度开始：自动恢复 +2 点
- "休息"行动：额外恢复 +12 点，消耗 1 行动点

**季度约束**：
- 每季度最大行动次数：8 次
- 玩家可在达到上限前主动点击"完成季度"
- 行动点用完时自动结束季度

---

## 二、行动系统设计

### 2.1 消耗行动点的操作

**基础行动**（全阶段可用）：

| 行动 | 图标 | 描述 | 消耗 |
|------|------|------|------|
| 做项目 | 🏗️ | 消耗资金和健康，推进项目进度 | 1 AP |
| 培训学习 | 📚 | 消耗资金，提升技能或健康 | 1 AP |
| 休息 | 😴 | 恢复健康 +12 点 | 1 AP |

**团队行动**（后期解锁）：

| 行动 | 图标 | 描述 | 消耗 |
|------|------|------|------|
| 招募成员 | 👥 | 招募团队成员 | 1 AP |
| 团队项目 | 🎯 | 委派团队执行项目（高效率） | 1 AP |
| 解决问题 | 🔧 | 处理团队问题，提升领导力 | 1 AP |

### 2.2 不消耗行动点的操作

**可随时访问**（通过底部 Tab）：
- 🤝 关系维护（"关系" Tab）
- 📊 材料交易（"市场" Tab）
- 📜 查看事件（"事件" Tab）

这些操作不受行动点限制，但仍受其他条件限制（现金、交易次数上限、维护次数上限）。

---

## 三、团队系统设计

### 3.1 团队成员类型

```typescript
enum TeamMemberType {
  ENGINEER = 'engineer',      // 工程师：提升项目质量
  SALESPERSON = 'salesperson', // 业务员：增加项目收益
  WORKER = 'worker',           // 劳务工：降低项目成本
  DESIGNER = 'designer',       // 设计师：提升项目效率
}
```

**成员属性**：
- `skill`: 技能等级 1-5
- `salary`: 季度工资
- `morale`: 士气 0-100
- `efficiency`: 效率 50-150%

### 3.2 团队效果计算

团队项目行动时生效：
```
质量加成 = 工程师人数 × 工程师技能 × 5
收益加成 = 业务员人数 × 业务员技能 × 3
成本降低 = 劳务工人数 × 劳务工技能 × 2
效率提升 = 设计师人数 × 设计师技能 × 4
```

### 3.3 解锁时机

- 晋升到项目经理后解锁
- 在季度结算页面点击"晋升"后启用
- 有明确的"进入新阶段"仪式感

---

## 四、领导力系统设计

### 4.1 领导力数值

- 范围：0-100
- 初始值：0（晋升项目经理时）

### 4.2 获取方式

| 方式 | 领导力提升 | 说明 |
|------|-----------|------|
| 解决团队问题 | +10 | 处理冲突、倦怠等 |
| 团队项目成功 | +5 | 项目完成且质量≥80 |
| 提升成员士气 | +2 | 通过"团队建设"行动 |
| 管理培训 | +3 | 消耗行动点的培训行动 |

### 4.3 领导力作用

1. **解决团队问题的要求**
   - 问题有"所需领导力"门槛
   - 领导力不足时无法解决某些高难度问题

2. **团队效率加成**
   - 每 1 点领导力 → 团队整体效率 +0.2%
   - 领导力 50 → 效率 +10%

3. **招募人数上限**
   - 基础上限：3 人
   - 每 10 点领导力 → +2 人上限
   - 领导力 50 → 最多 13 人

4. **晋升合伙人条件**
   - 领导力 ≥ 60 是晋升合伙人的必要条件之一

---

## 五、事件系统重构

### 5.1 事件触发机制

**触发方式**（独立计数器）：
```
每消耗 2 点行动点 → 触发事件检测
  ├─ 50% 概率：弹出事件窗口
  ├─ 50% 概率：无事发生
  └─ 计数器重置，继续累计
```

### 5.2 事件窗口操作

| 选项 | 效果 | 行动点消耗 |
|------|------|-----------|
| 立即处理 | 获得事件奖励/惩罚 | 0 |
| 延后处理 | 加入待处理队列，2 回合内必须处理 | 0 |
| 忽略 | 跳过事件，可能产生负面后果 | 0 |

### 5.3 待处理事件

- 状态栏显示提醒图标："🔔 待处理: 2"
- 延后的事件有截止时间（2 次行动后）
- 超时未处理自动应用惩罚

### 5.4 紧急事件

- 部分事件标记为"紧急"（红色标识）
- 紧急事件必须立即处理，无法延后

### 5.5 不消耗行动点

- 事件不消耗行动点，不影响玩家行动节奏
- 可以在做项目途中弹出

---

## 六、游戏结束条件

### 6.1 失败条件调整

**移除现金不足导致游戏结束**：

| 条件 | 状态 | 说明 |
|------|------|------|
| 现金 ≤ 0 | ❌ 移除 | 不再导致游戏结束 |
| 健康 ≤ 0 | ✅ 保留 | 过劳猝死 |
| 声誉 ≤ 0 | ✅ 保留 | 行业封杀 |

### 6.2 现金为 0 时的影响

- 无法执行消耗现金的行动（做项目、材料交易、关系维护）
- 只能执行"休息"行动恢复健康
- 健康 → 行动点 → 休息 → 健康...
- 形成困境循环，但不强制结束游戏

### 6.3 胜利条件

晋升到合伙人，需要满足：
- 净资产 ≥ 5000 万
- 完成项目数 ≥ 40
- 声誉 ≥ 95
- 优质项目数 ≥ 10
- **领导力 ≥ 60**（新增条件）

---

## 七、UI 结构设计

### 7.1 主游戏界面布局

```
┌─────────────────────────────────────┐
│  顶部状态栏（固定）                    │
│  👔工程师 | ❤️80 | ⭐60 | 💰50000    │
├─────────────────────────────────────┤
│                                     │
│  主内容区域（滚动）                   │
│  - 根据底部导航显示不同页面            │
│                                     │
├─────────────────────────────────────┤
│  底部导航栏（固定）                    │
│  [行动] [团队🔒] [市场] [关系] [事件] │
└─────────────────────────────────────┘
```

### 7.2 五个导航页面

| 导航 | 路由 | 页面 | 说明 |
|------|------|------|------|
| 行动 | /game/actions | ActionsPage | 显示可用行动列表 |
| 团队 | /game/team | TeamPage | 团队管理（后期解锁） |
| 市场 | /game/market | MarketPage | 材料市场交易 |
| 关系 | /game/relations | RelationsPage | 关系维护 |
| 事件 | /game/events | EventsPage | 事件历史和待处理 |

### 7.3 解锁状态

- 前期（实习生-高级工程师）：团队标签显示 🔒
- 后期（项目经理及以上）：解锁团队页面

---

## 八、ActionsPage 界面

### 8.1 页面布局

```
┌─────────────────────────────────────┐
│  第 5 季度                           │
│  行动点：3/5                         │
│  健康：60/100                        │
├─────────────────────────────────────┤
│  可用行动（消耗 1 行动点）：          │
│                                     │
│  ┌──────────┐ ┌──────────┐          │
│  │ 🏗️ 做项目 │ │ 📚 培训  │          │
│  │ 消: 5000 │ │ 消: 8000 │          │
│  │ 进+10 质+5│ │ 技能+8   │          │
│  └──────────┘ └──────────┘          │
│  ┌──────────┐                       │
│  │ 😴 休息  │                       │
│  │ 健康+12  │                       │
│  └──────────┘                       │
│                                     │
│  团队行动（后期解锁）：                │
│  ┌──────────┐ ┌──────────┐          │
│  │ 👥 招募  │ │ 🎯 团队项目│          │
│  │ 🔒       │ │ 🔒       │          │
│  └──────────┘ └──────────┘          │
│                                     │
│  [ 完成本季度 ]                     │
└─────────────────────────────────────┘
```

### 8.2 状态信息

- 当前季度数
- 行动点（X/5）= Math.ceil(健康/20)
- 当前健康值

### 8.3 行动点用完

- 自动进入季度结算
- 无需手动点击"完成季度"（但仍可提前结束）

---

## 九、类型定义设计

### 9.1 团队相关类型

```typescript
// shared/types/team.ts（新建）

export enum TeamMemberType {
  ENGINEER = 'engineer',
  SALESPERSON = 'salesperson',
  WORKER = 'worker',
  DESIGNER = 'designer',
}

export interface TeamMember {
  id: string;
  type: TeamMemberType;
  name: string;
  skill: number;        // 1-5
  salary: number;       // 季度工资
  morale: number;       // 0-100
  efficiency: number;   // 50-150%
}

export interface TeamIssue {
  id: string;
  type: 'conflict' | 'burnout' | 'mistake' | 'demand';
  description: string;
  severity: 'low' | 'medium' | 'high';
  affectedMember?: string;
  requiredLeadership: number;
  resolveReward: {
    leadership?: number;
    efficiency?: number;
    morale?: number;
  };
}

export interface TeamState {
  members: TeamMember[];
  leadership: number;
  teamEfficiency: number;
  pendingIssues: TeamIssue[];
}
```

### 9.2 扩展 GameState

```typescript
// shared/types/game.ts

export enum GamePhase {
  EARLY = 'early',   // 实习生-高级工程师
  LATE = 'late',     // 项目经理-合伙人
}

export interface GameState {
  // ... 现有字段
  phase: GamePhase;
  actionPoints: number;
  maxActionPoints: number;
  team: TeamState;
  pendingEvents: EventCard[];
  currentQuarter: number;
  maxActionsPerQuarter: number;
}
```

---

## 十、数据流与状态管理

### 10.1 游戏核心流程

```
PLAYING（行动页面）
  ├─ 执行行动 → 消耗 1 行动点
  │
  ├─ 切换到"关系"Tab → 不消耗行动点
  │   └─ 返回"行动"Tab → 继续行动
  │
  ├─ 切换到"市场"Tab → 不消耗行动点
  │   └─ 返回"行动"Tab → 继续行动
  │
  ├─ 行动点归零 → 自动进入季度结算
  │
  └─ 事件触发 → 弹窗 → 不消耗行动点
```

### 10.2 关键 Store 方法

```typescript
interface GameStore {
  // 行动系统
  doAction: (actionType: ActionType) => ActionResult;

  // 季度系统
  finishQuarter: () => void;
  nextQuarter: () => void;

  // 团队系统
  recruitMember: (memberType: TeamMemberType) => RecruitmentResult;
  resolveTeamIssue: (issueId: string) => ResolveResult;
  updateTeamEfficiency: () => void;

  // 事件系统
  checkEventTrigger: () => void;
  deferEvent: (eventId: string) => void;
  ignoreEvent: (eventId: string) => void;
}
```

---

## 十一、实施计划

### Phase 1: 类型定义扩展
- 创建 `shared/types/team.ts`
- 扩展 `shared/types/game.ts`
- 扩展 `shared/types/event.ts`

### Phase 2: 常量配置
- 添加行动配置（ACTIONS）
- 添加团队配置（RECRUIT_CONFIG、LEADERSHIP_GAIN等）
- 更新游戏结束条件

### Phase 3: 状态管理重构
- 重构 `gameStore.ts`
- 添加行动点系统
- 添加团队系统
- 添加事件系统重构

### Phase 4: 页面组件
- 创建 `MainGame.tsx`（主容器）
- 创建 `ActionsPage.tsx`
- 创建 `TeamPage.tsx`
- 拆分 `StrategyPhase.tsx` → `MarketPage.tsx` + `RelationsPage.tsx`
- 创建 `EventsPage.tsx`

### Phase 5: UI 组件
- `BottomNav.tsx`
- `TopStatusBar.tsx`
- `ActionCard.tsx`
- `TeamMemberCard.tsx`
- `EventModal.tsx`

### Phase 6: 路由和集成
- 更新 `App.tsx` 路由配置
- 集成所有页面
- 测试完整流程

---

## 十二、新增文件清单

```
shared/types/
  └── team.ts                          # 团队系统类型

frontend/src/pages/
  ├── MainGame.tsx                      # 主游戏容器
  ├── ActionsPage.tsx                   # 行动页面
  ├── TeamPage.tsx                      # 团队页面
  ├── MarketPage.tsx                    # 市场页面（从 StrategyPhase 拆分）
  ├── RelationsPage.tsx                 # 关系页面（从 StrategyPhase 拆分）
  └── EventsPage.tsx                    # 事件页面

frontend/src/components/
  ├── BottomNav.tsx                     # 底部导航栏
  ├── TopStatusBar.tsx                  # 顶部状态栏
  ├── ActionCard.tsx                    # 行动卡片
  ├── TeamMemberCard.tsx                # 团队成员卡片
  ├── EventModal.tsx                    # 事件弹窗
  └── ActionPointsBar.tsx               # 行动点条
```

---

## 十三、删除文件清单

```
frontend/src/pages/
  ├── Game.tsx                          # 被 MainGame.tsx 替代
  └── StrategyPhase.tsx                 # 拆分为 MarketPage 和 RelationsPage
```
